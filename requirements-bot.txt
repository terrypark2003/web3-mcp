# Extra dependencies for the Telegram bot + live execution.
# The core scanner (requirements.txt) needs only `requests`; these are required
# only if you run the Telegram bot or place live orders.
# [job-queue] extra powers the proactive alert loop.
python-telegram-bot[job-queue]>=20,<22
# Polymarket rejects orders signed by old clients ("invalid order version, please
# use the latest clob-client"). Pin a modern floor — verified API-compatible with
# our usage (ClobClient/OrderArgs/create_and_post_order). Bumping this string also
# busts Docker's pip layer cache so a redeploy actually reinstalls the new version.
py-clob-client>=0.34
