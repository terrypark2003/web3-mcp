# Extra dependencies for the Telegram bot + live execution.
# The core scanner (requirements.txt) needs only `requests`; these are required
# only if you run the Telegram bot or place live orders.
# [job-queue] extra powers the proactive alert loop.
python-telegram-bot[job-queue]>=20,<22
# Official unified Polymarket SDK (repo Polymarket/py-sdk) — the ONLY official
# client that can place CLOB V2 orders for "deposit wallet" (email/Magic)
# accounts (ERC-7739 POLY_1271 signing + relayer integration). py-clob-client
# and py-clob-client-v2 are deprecated upstream and reject deposit-wallet makers
# with "maker address not allowed". Beta: pin exact (an exact pre-release pin
# installs without --pre). Requires Python >= 3.11 (Dockerfile uses 3.12).
polymarket-client==0.1.0b11
